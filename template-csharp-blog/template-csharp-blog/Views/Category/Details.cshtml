@model template_csharp_blog.Models.Category

@{
    ViewData["Title"] = "Details";
}

<h1>@Model.Name</h1>

<div>
    <h4>Category</h4>
    <hr />
@*    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>*@
    </dl>
</div>
@*<table>
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Posts.Name)
            </th>
        </tr>
    </thead>
</table>*@
@foreach(Post p in Model.Posts)
{
    <h2 id="titleDetailStyle">@p.Title</h2>
    <h3>By: <h3 id="authorDetailStyle">@p.Author</h3></h3> 
    <p id="bodyDetailStyle">@p.Body</p>
}
<div class="detailsLinks">
    @*<a asp-action="Update" asp-route-id="@Model?.Id">Edit Name</a> |*@
    <a asp-action="Index">Back to categories</a>
</div>
